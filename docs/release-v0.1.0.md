# v0.1.0 release record

This document is reserved for the immutable v0.1.0 release record. The final
tag, commit, release asset names, asset sizes, and SHA-256 digests are filled
after the protected GitHub Actions workflow passes and the release asset has
been uploaded and re-read from GitHub.

The release does not grant protected-core adoption authority.
